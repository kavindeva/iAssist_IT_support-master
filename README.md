# iAssist_IT_support

1. This repository is same as IT support chatbot repositroy created for production server deployment with only required files and folders.
2. Created a new Virtual Environment and installed all required packages. So no need to install any additional packages.

## Python version = 3.7.9
